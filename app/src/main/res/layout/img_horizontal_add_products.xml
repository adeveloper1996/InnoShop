<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_marginRight="@dimen/dp_5"
    android:gravity="center">

    <ImageView
        android:layout_width="100dp"
        android:layout_height="80dp"
        app:srcCompat="@mipmap/ic_launcher"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:id="@+id/image_horizontal_add_products"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_clear_grey_500_24dp"
        android:background="@drawable/white_radius_indicator"
        android:id="@+id/i_btn_delete_image"
        android:layout_alignTop="@+id/image_horizontal_add_products"
        android:layout_alignRight="@+id/image_horizontal_add_products"
        android:layout_alignEnd="@+id/image_horizontal_add_products" />
</RelativeLayout>